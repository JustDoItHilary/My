<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testrecyclerviewdemo">

    <uses-sdk
        android:minSdkVersion="21"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"

        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MyVibrateActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MyDividerActivity" ></activity>
        <activity android:name=".MyDividerRecyclerViewActivity" ></activity>
        <activity android:name=".MySurfaceViewActivity" />
        <activity android:name=".MyHttpActivity" />
        <activity android:name=".MyVolleyActivity" />
        <activity android:name=".MyOkHttp2Activity" />
        <activity android:name=".MyURLWebActivity">

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="bb"
                    android:scheme="aa" />
            </intent-filter>
        </activity>
        <activity android:name=".MyRippleActivity"></activity>
    </application>

</manifest>